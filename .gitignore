# Auth & secrets
auths/
data/
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json
# /admin 在线保存配置时自动生成的备份（含 api_key / upstash token）
config.json.bak

# Build artifacts
*.tmp
__pycache__/
# 打包产物目录（含 exe / 复制进去的 config.json 与 auths/）
dist/
dist-linux/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md
docs/

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md
